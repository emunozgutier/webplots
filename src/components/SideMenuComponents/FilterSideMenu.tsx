import React from 'react';

const FilterSideMenu: React.FC = () => {
    return (
        <div className="p-3">
            <h5 className="mb-3">Filters</h5>
            <p className="text-muted small">Filter configuration will go here.</p>
        </div>
    );
};

export default FilterSideMenu;
